# GENERATED BY scripts/platform-render.sh. DO NOT EDIT.
# Source: ../app.yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: demo
  namespace: rag
spec:
  interval: 10m
  timeout: 10m
  releaseName: demo
  chart:
    spec:
      chart: ./clusters/homelab/charts/service-template
      sourceRef:
        kind: GitRepository
        name: homelab
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
      remediateLastFailure: true
  valuesFrom:
    - kind: ConfigMap
      name: demo-values-generated
      valuesKey: values.yaml
